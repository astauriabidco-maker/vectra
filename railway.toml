[build]
builder = "NIXPACKS"
buildCommand = "yarn turbo build --filter=api-core"

[deploy]
startCommand = "node apps/api-core/dist/src/main.js"
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
maxRestartCount = 3
